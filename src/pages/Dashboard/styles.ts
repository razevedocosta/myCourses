import styled from "styled-components";

export const Content = styled.div`
    max-width: 960px;
    margin: 0 auto;

    padding: 0 5rem;

    > div {
        margin: 1rem 0;
    }

    section {
        display: flex;
        align-items: center;
        justify-content: flex-start;
        flex-wrap: wrap;
    }
`;